import React from "react";

const BigInput = () => {
  return (
    <div className="big-input-block">
      <input
        className="big-input"
        type="text"
        placeholder="Тема..."
        id="subject"
        name="subject"
      />
    </div>
  );
};

export default BigInput;
